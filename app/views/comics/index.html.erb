<h3>Archive</h3>

<ul>
  <% @comics.each do |comic| %>
    <li>
      <%= image_tag(comic.uri, height: '32', width: '32') %>
      <strong>
        <%= link_to "#{comic.name}", comic_path(comic) %>
      </strong> - <%= comic.created_at %>
    </li>

  <% end %>
</ul>
